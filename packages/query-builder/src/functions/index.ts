import { from } from "./from"
import { op } from "./op"
import { select } from "./select"
import { where } from "./where"

export const functions = {
  from: from,
  op: op,
  select: select,
  where: where,
}
